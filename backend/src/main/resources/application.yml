spring:
  application:
    name: backend
  
  datasource:
    url: jdbc:postgresql://localhost:5433/PSYCareDB
    username: postgres
    password: 123Andrei
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: none